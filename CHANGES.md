# Borshevik Image Manager — Документация изменений

**Дата**: 17 февраля 2026  
**Репозиторий**: [komorebinator/borshevik](https://github.com/komorebinator/borshevik)  
**Директория**: `build_files/root/usr/share/borshevik-image-manager/`

---

## Обзор проекта

Borshevik Image Manager — GTK4/Adwaita приложение на GJS (JavaScript for GNOME) для управления обновлениями образов через `rpm-ostree`. Приложение позволяет проверять обновления, применять их, откатываться на предыдущий образ, перезагружаться и выполнять rebase на другой образ/канал.

---

## Проблема 1: Прогресс rebase отображался в окне настроек

### Было
При нажатии «Apply» в настройках (rebase на другой образ), прогресс выполнения отображался **внутри окна Settings** — в специальном busy-view с прогресс-баром и консольным выводом. При этом кнопки «Apply» и «Reset» оставались в заголовке окна настроек, а главное окно приложения оставалось в обычном состоянии.

```
Settings Window:
  [Reset] [Apply]          ← активны, хотя операция идёт
  ┌──────────────────────┐
  │ Rebasing…            │ ← прогресс здесь
  │ ████████░░░░░░░░░░░░ │
  │ > rpm-ostree rebase… │
  └──────────────────────┘
```

### Проблемы
- Пользователь мог закрыть окно Settings во время rebase
- Кнопки Apply/Reset оставались кликабельными
- Два разных busy-view (Settings и MainWindow) — дублирование кода

### Стало
Settings больше не выполняет rebase самостоятельно. Вместо этого:

1. `settings_window.js` эмитит сигнал `'rebase-requested'` с целевым ref
2. Окно Settings закрывается (через `GLib.idle_add` для безопасности)
3. `main_window.js` ловит сигнал и запускает `_doRebase(targetRef)` в **своём** busy-view
4. Из `settings_window.js` удалены: `_buildBusyView()`, `_enterBusy()`, `_leaveBusy()`, `_appendOutput()`, импорты `CommandRunner` и `isAuthorizationError`

```
Settings → emit('rebase-requested', ref) → close()
                    ↓
MainWindow → _doRebase(ref) → busy view (единый для всех операций)
```

### Изменённые файлы
- `settings_window.js` — удалён busy-view, новый сигнал `'rebase-requested'`
- `main_window.js` — добавлен `_doRebase()`, подключение к сигналу

---

## Проблема 2: Невнятные заголовки при выполнении операций

### Было
Во время обновления заголовок busy-view показывал `"Update"`, во время rebase — `"Apply"`, во время отката — `"Rollback"`. Это просто названия кнопок, а не описание того, что происходит.

### Стало
Новые описательные заголовки:
- Обновление: **«Applying update…»** / **«Применение обновления…»**
- Откат: **«Rolling back…»** / **«Откат…»**
- Rebase: **«Rebasing…»** / **«Rebase…»**
- Promote: **«Promoting to stable…»** / **«Продвижение в stable…»**

### Изменённые файлы
- `main_window.js` — новые вызовы `_enterBusy(i18n.t('busy_applying_update'))` и т.д.
- `en.json`, `ru.json` — добавлены ключи `busy_applying_update`, `busy_rolling_back`, `busy_rebasing`, `busy_promoting`

---

## Проблема 3: Плохой консольный вывод

### Было
`CommandRunner` читал вывод процесса **построчно** через `Gio.DataInputStream.read_line_async()`. Это означало:
- Данные появлялись только при получении символа `\n`
- rpm-ostree использует `\r` (carriage return) для обновления строки прогресса на месте → эти обновления не отображались до завершения блока
- Прогресс-бар всегда пульсировал (indeterminate), не показывая реальный прогресс скачивания

```
// Старый подход — read_line_async:
"Fetching metadata..."    ← появляется
(долгая пауза)            ← \r-обновления не видны
"Done."                   ← появляется только в конце
```

### Стало

**CommandRunner** — чтение чанками:
- `read_bytes_async(4096)` на сыром `GInputStream` вместо `DataInputStream`
- Текст доставляется немедленно, включая середину строки

**_appendOutput()** — обработка `\r`:
- Входной текст разбивается на части по `\r`, `\n`, `\r\n`
- На `\r`: удаляется контент текущей строки в TextBuffer (имитация терминала)
- На `\n`: вставляется новая строка

**Прогресс-бар** — парсинг чанков:
- Паттерн `N/M chunks` → переключение в determinate режим (`fraction = N/M`)
- При фазах Importing/Writing/Staging → возврат в pulse режим

**Авто-прокрутка**:
- После каждого вывода — `GLib.idle_add` прокрутка TextBuffer вниз

### Изменённые файлы
- `command_runner.js` — полностью переписан механизм чтения
- `main_window.js` — новый `_appendOutput()`, `_tryParseChunkProgress()`, `_autoScrollToBottom()`

---

## Проблема 4: Нет функции «Promote to stable»

### Было
Возможность продвинуть текущий образ в stable-канал отсутствовала. GitHub Actions workflow `promote-image-to-stable.yml` существовал в репозитории, но вызвать его можно было только вручную через GitHub.

### Стало
В бургер-меню динамически появляется пункт **«Promote this image to stable»**, если:
- Текущий канал = `latest`
- Вариант = `standard` или `nvidia`

При клике:
1. Диалог подтверждения
2. Проверка наличия `gh` CLI (`which gh`)
3. Извлечение дайджеста образа из `rpm-ostree status --json`
4. Запуск `gh workflow run promote-image-to-stable.yml -R komorebinator/borshevik -f variant=... -f digest=...`
5. Прогресс в busy-view MainWindow

Если `gh` не установлен — информативное сообщение об ошибке.

### Новые функции
- `rpm_ostree.js` → `extractDigest(deployment)` — извлечение SHA256 дайджеста
- `rpm_ostree.js` → `deriveVariantName(origin)` — определение `'borshevik'` / `'borshevik-nvidia'`
- `main_window.js` → `_updatePromoteMenuItem()` — динамическое управление пунктом меню
- `main_window.js` → `_onPromoteClicked()` — подтверждение + запуск workflow

### Изменённые файлы
- `main_window.js` — новая функция promote
- `rpm_ostree.js` — вспомогательные функции
- `en.json`, `ru.json` — 6 новых ключей для promote

---

## Проблема 5: «Check for updates» не показывает обновления при наличии staged

### Было
В `_checkForUpdates()` была логика:
```javascript
if (hasStaged) {
  this._check = { phase: 'idle', downloadSize: null, message: '' };
}
```
Если обновление уже скачано и ожидает перезагрузки (`staged`), новое, ещё более свежее обновление **скрывалось**. Пользователь видел только «Reboot to apply update», не зная, что есть что-то новее.

### Стало

**`main_window.js`**: удалена ветка `if (hasStaged)`. Теперь:
```javascript
if (hasAvailableUpdate) {
  this._check = { phase: 'available', downloadSize: size, message: '' };
} else {
  this._check = { phase: 'no_updates', downloadSize: null, message: '' };
}
```

**`app_state.js`**: `computeUiState()` теперь обрабатывает комбинацию `needsReboot + available`:
- `primaryMode = 'update'` доступен даже при `needsReboot`
- Специальный текст статуса: «A newer update is available (previous update is pending reboot).»

### Изменённые файлы
- `main_window.js` — убрана ветка suppressions
- `app_state.js` — новая логика `primaryMode` + комбинированный статус

---

## Проблема 6: Ненадёжный механизм переводов (i18n)

### Было
`i18n.js` загружал строки **только** из JSON-файлов (`en.json`, `ru.json`). Если файл отсутствовал, был повреждён, или ключ не существовал — в интерфейсе отображался **сырой ID ключа** (например, `primary_check` вместо «Check for updates»).

```javascript
t(key) {
  if (key in this._dict) return this._dict[key];
  if (key in this._en) return this._en[key];
  return key;  // ← сырой ID без предупреждения
}
```

### Стало
Английские строки **встроены в код** как `DEFAULT_STRINGS` — они всегда доступны, даже если все JSON-файлы удалены.

Цепочка поиска (3 уровня + fallback):
```
Locale JSON (ru.json) → en.json → DEFAULT_STRINGS → key + console.warn
```

При отсутствии ключа выводится предупреждение в консоль для отладки:
```
[i18n] WARNING: Missing translation key: "some_key"
```

При невалидном JSON (синтаксическая ошибка) — лог предупреждения, fallback к defaults:
```
[i18n] WARNING: Invalid JSON in /path/to/ru.json: Unexpected token...
```

### Изменённые файлы
- `i18n.js` — полная переработка с `DEFAULT_STRINGS`

---

## Ревизия №2: Критический обзор и исправления (17 февраля 2026)

После внедрения 6 issues проведён критический обзор кода, выявлено **9 проблем** (3 критических бага, 4 архитектурных, 2 по качеству кода). Все исправлены.

### 🔴 Критические баги

| ID | Файл | Проблема | Исправление |
|----|------|----------|-------------|
| BUG-1 | `main_window.js:624` | `get_iter_at_line()` в GTK4 возвращает `[bool, Iter]`, а не `Iter` — crash при каждом `\r` в консоли | Деструктуризация: `const [, lineStart] = ...` |
| BUG-2 | `command_runner.js` | `TextDecoder` без `{ stream: true }` — обрезка многобайтных UTF-8 символов (кириллица) на границе 4096-байтного чанка | Отдельный `TextDecoder` на поток + `{ stream: true }` + финализирующий `decode()` |
| BUG-3 | `command_runner.js` | `return` в `try` резолвил промис до `finally` → `onExit` / `_leaveBusy()` вызывался с задержкой (race condition) | Убран `try/finally`, линейный поток: drain streams → `onExit()` → `return` |

### 🟡 Архитектурные исправления

| ID | Файл | Проблема | Исправление |
|----|------|----------|-------------|
| ARCH-1 | `main_window.js` | `Gio.Menu.insert(0)`/`remove(0)` сдвигал индексы Settings/About | Отдельная `_promoteSection` через `append_section()` — изоляция от основного меню |
| ARCH-2 | `main_window.js` | `_doRebase()` мог вызваться на разрушенном MainWindow | Guard `if (!this._stack) return` |
| ARCH-3 | `main_window.js` | `_onPromoteClicked()` не собирал `collected` output — нарушение паттерна | Добавлен `collected` как в `_doUpgrade`/`_doRollback`/`_doRebase` |
| ARCH-4 | `rpm_ostree.js` | Regex `sha256:[0-9a-fA-F]+` мог захватить неполный digest | Строгий `sha256:[0-9a-fA-F]{64}` |

### 🔵 Качество кода

| ID | Файл | Проблема | Исправление |
|----|------|----------|-------------|
| QA-1 | `main_window.js` | `_autoScrollToBottom()` — `GLib.idle_add` на каждый чанк без debounce | Флаг `_scrollPending` — максимум один scroll в очереди |
| QA-3 | `main_window.js` | Regex `(\d+)/(\d+)\s*(chunks)` мог ложно срабатывать | Требуется контекст: `(?:Fetching\|Downloading\|Receiving)` перед `N/M` |

---

## Ревизия №3: Сравнение с upstream и исправления (17 февраля 2026)

Полное сравнение локального кода со всеми 9 файлами из upstream [komorebinator/borshevik](https://github.com/komorebinator/borshevik/tree/main/build_files/root/usr/share/borshevik-image-manager). Выявлено **5 проблем** (2 критических, 1 moderate, 2 quality). Все исправлены.

### 🔴 Критические баги

| ID | Файл | Проблема | Исправление |
|----|------|----------|-------------|
| BUG-1 | `main_window.js` | `onExit` callback в `CommandRunner` срабатывал после **первого** `runner.run()` — при auth-retry (pkexec) `_leaveBusy()` вызывался преждевременно, кнопка Close становилась активной пока вторая команда ещё работала | Убран `onExit` из конструктора `CommandRunner`. `_leaveBusy()` вызывается явно **после** всех `runner.run()`. `collected` сбрасывается перед retry. Применено к 4 методам: `_doUpgrade`, `_doRollback`, `_doRebase`, `_onPromoteClicked` |
| BUG-2 | `command_runner.js` | После drain streams `status` перечитывался через `get_exit_status()`, но `success` оставался от предыдущего вызова — рассинхронизация | `success` пересчитывается через `get_successful()` вместе со `status` в том же try-блоке |

### 🟡 Moderate

| ID | Файл | Проблема | Исправление |
|----|------|----------|-------------|
| BUG-5 | `main_window.js` | `_onPromoteClicked()` не вызывал `_refreshStatus()` в конце — нарушение паттерна, принятого в `_doUpgrade`/`_doRollback`/`_doRebase` | Добавлен `await this._refreshStatus()` в конце метода |

### 🔵 Качество кода

| ID | Файл | Проблема | Исправление |
|----|------|----------|-------------|
| QA-2 | `i18n.js` | `_getLocale()` не проверял `LANGUAGE` env var — в GNOME `LANGUAGE` (напр. `ru:en`) имеет приоритет над `LC_MESSAGES`/`LANG` | Добавлен `GLib.getenv('LANGUAGE')` как первый приоритет. Существующий regex корректно извлекает первый 2-буквенный код из списка |
| QA-5 | `main_window.js` | Regex прогресса чанков не ловил `Pulling` — возможный keyword rpm-ostree | Добавлен `Pulling` в альтернативы: `(?:Fetching\|Downloading\|Receiving\|Pulling)` |

---

## Сводная таблица изменений

| Файл | Было (строк) | Стало (строк) | Суть |
|------|-------------|--------------|------|
| `i18n.js` | 47 | 136 | Встроенные дефолты, 3-уровневый fallback |
| `command_runner.js` | 106 | 115 | Чанковое чтение, per-stream TextDecoder, линейный flow |
| `main_window.js` | 720 | ~925 | CR-handling, chunk progress, promote, rebase, section-menu, guards |
| `settings_window.js` | 418 | 317 | Удалён busy-view, сигнал `'rebase-requested'` |
| `app_state.js` | 84 | 89 | Обновления при staged, комбинированный статус |
| `rpm_ostree.js` | 212 | 240 | `extractDigest()`, `deriveVariantName()`, strict SHA256 regex |
| `en.json` | 60 | 70 | +8 новых ключей |
| `ru.json` | 60 | 70 | +8 новых ключей с переводами |

---

## Архитектурные решения

### Единый busy-view
Все долгие операции (update, rollback, rebase, promote) проходят через **один** busy-view в MainWindow. Settings больше не имеет своего busy-view.

### Chunk-based reading
`read_bytes_async(4096)` вместо `read_line_async()` — более низкоуровневый, но более надёжный и портативный API для GJS. Каждый поток (stdout/stderr) имеет свой `TextDecoder` с `{ stream: true }` для корректной обработки многобайтных символов.

### Секционное динамическое меню
Пункт «Promote to stable» управляется через отдельную `Gio.Menu` секцию (`_promoteSection`), подключённую через `append_section()`. Это изолирует промо-пункт от основных элементов (Settings, About) и исключает сдвиг индексов.

### Встроенные дефолты i18n
`DEFAULT_STRINGS` — единственный источник истины для английских строк. JSON-файлы являются **опциональными переопределениями**.

---

## Тестирование

Полная тестовая матрица (28 сценариев) доступна в `implementation_plan.md`. Ключевые категории:
- **14 регрессионных тестов** — все существующие функции
- **14 тестов новых функций** — каждый issue
- **10 edge-case** — удалённые JSON, нет rpm-ostree, отмена pkexec, закрытие окна во время операции

> **Требуется**: запуск на системе с Borshevik для полного runtime-тестирования.
